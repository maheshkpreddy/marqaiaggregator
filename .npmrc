# Allow npm install to succeed even when peer deps (React 19, Next 16,
# Radix UI) have version-range conflicts. Without this, Vercel's install
# step fails because there is no checked-in lockfile to pin resolutions.
legacy-peer-deps=true
