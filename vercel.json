{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "echo 'diag: skipping vercel-build.sh' && npx prisma generate && npx next build",
  "installCommand": "npm install --legacy-peer-deps",
  "regions": ["bom1"],
  "env": {
    "NEXT_PUBLIC_APP_NAME": "Marq AI Aggregator"
  }
}
